# 📘 گزارش کامل پروژه (گام‌های 1 تا 4)

#  سیستم رزرو هتل — گزارش مراحل تمرین OOD و SOLID

این مخزن شامل دو بخش اصلی است:

* **Step-01-without-OOD** → اضافه کردن قابلیت‌ها بدون رعایت اصول شی‌گرایی
* **Step-02-with-OOD** → بازطراحی کامل کد بر اساس اصول SOLID و معماری تمیز

در ادامه گزارش کامل گام‌های تمرین آورده شده است.

---

#  مرحله ۱ — آشنایی با پروژه و اجرای آن

در این مرحله:

* مخزن پروژه‌ی پایه کلون شد.
* ساختار پروژه بررسی شد (ReservationService، PaymentProcessor، EmailSender و …).
* پروژه با ابزار Maven اجرا شد.
* نیازمندی‌ها و ساختار موجود پروژه تحلیل شد.

### هدف این مرحله

درک پروژه و اجرای اولیه برای ادامه‌ی توسعه.

---

#  مرحله ۲ — افزودن قابلیت‌ها **بدون اعمال OOD (مرحله اول تمرین)**

در این مرحله دو قابلیت جدید، بدون توجه به اصول طراحی شی‌گرا، اضافه شد:

---

## ✔ افزودن ارسال پیامک (SMS)

### فایل جدید:

`SmsMessageSender.java`

### عملکرد:

* مشابه EmailSender
* پیامک را با چاپ در کنسول شبیه‌سازی می‌کند

### تغییر در ReservationService:

یک case جدید در switch مربوط به Notifier اضافه شد:

```java
SmsMessageSender sms = new SmsMessageSender();
sms.sendSms(res.customer.phone, "Your reservation confirmed by SMS!");
```

---

## ✔ افزودن روش پرداخت حضوری (On-Site Payment)

### فایل جدید:

`OnSitePaymentProcessor.java`

### عملکرد:

پرداخت حضوری را با چاپ در کنسول شبیه‌سازی می‌کند.

### تغییر در ReservationService:

یک case جدید در switch مربوط به PaymentMethods اضافه شد:

```java
OnSitePaymentProcessor onSite = new OnSitePaymentProcessor();
onSite.payAtLocation(res.totalPrice());
```

---

## ⚠ مشکلات باقی‌مانده از عمد

در این مرحله عمدی اصول SOLID رعایت نشد:

* وابستگی مستقیم ReservationService به کلاس‌های concrete
* استفاده از switch-case برای انتخاب پیام‌رسان و پرداخت
* چند مسئولیتی بودن کلاس‌ها
* عدم استفاده از Interface مناسب
* تغییر مستقیم کلاس برای افزودن قابلیت جدید

این‌ها دقیقاً پایه‌ی ریفکتور مرحله دوم هستند.

---

#  مرحله ۳ — بازطراحی پروژه با **اصول OOD + SOLID**

در این گام کل سیستم با اصول طراحی شی‌گرا بازطراحی شد.

---

# ✔️ اعمال اصل SRP — Single Responsibility Principle

* کلاس پیام‌رسانی جدا شد → MessageSender
* کلاس‌های EmailSender و SmsSender مسئولیت واحد دارند.
* کلاس‌های پرداخت جدا شدند → PaymentProcessor و مشتقاتش
* فاکتور چاپ کردن در یک متد جداگانه
* ReservationService فقط مسئول ایجاد رزرو است.

---

# ✔️ اعمال اصل OCP — Open/Closed Principle

افزودن یک روش پرداخت جدید (مثلاً Bitcoin):

```java
public class BitcoinPaymentProcessor implements PaymentProcessor { ... }
```

بدون تغییر حتی **یک خط** در ReservationService انجام می‌شود.

همین‌طور برای اضافه‌کردن پیام‌رسان جدید (WhatsAppSender و …)

---

# ✔️ اعمال DIP — Dependency Inversion Principle

ReservationService به abstraction وابسته شد:

```java
private final PaymentProcessor paymentProcessor;
private final MessageSender messageSender;
```

و این وابستگی‌ها در constructor تزریق می‌شوند:

```java
public ReservationService(PaymentProcessor paymentProcessor, MessageSender messageSender)
```

---

# ✔️ اعمال ISP — Interface Segregation Principle

به‌جای یک interface بزرگ:

* MessageSender فقط متد send دارد
* PaymentProcessor فقط pay دارد

هیچ کلاسی مجبور به پیاده‌سازی متدهای اضافی نیست.

---

# ✔️ اعمال LSP — Liskov Substitution Principle

همه PaymentProcessorها کاملاً قابل جایگزینی هستند:

* CardPaymentProcessor
* PayPalPaymentProcessor
* CashPaymentProcessor
* OnSitePaymentProcessor

ReservationService نمی‌فهمد کدام یک دارد استفاده می‌شود—فقط یک PaymentProcessor می‌بیند.

---

#  نتیجه‌گیری نهایی

در این پروژه:

### 🔹 مرحله ۱

قابلیت‌های جدید بدون توجه به OOD اضافه شد (پیامک + پرداخت حضوری).

### 🔹 مرحله ۲

کل پروژه بر پایه‌ی SOLID بازطراحی شد:

| اصل | قبل ریفکتور              | بعد ریفکتور                                |
| --- | ------------------------ | ------------------------------------------ |
| SRP | شدیداً نقض               | کاملاً برقرار                              |
| OCP | نقض                      | اضافه کردن قابلیت بدون تغییر کد موجود      |
| LSP | ناقص                     | همه پرداخت‌ها و پیام‌رسان‌ها قابل جایگزینی |
| ISP | نقض                      | هر Interface کوچک و تخصصی                  |
| DIP | وابستگی به کلاس concrete | وابستگی به abstraction                     |

---

